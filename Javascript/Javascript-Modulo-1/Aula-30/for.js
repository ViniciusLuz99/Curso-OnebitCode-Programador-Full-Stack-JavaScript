let indice = 0;
// inicialização; condição; finalização
for (alert("Inicializando o for"); indice <= 10; indice++) {
  alert("Indice = " + indice);
}
let nome = "Nome nome" //tamanho -> 0, 1, 2, 3, 4,....
//i = indice
for(let i=0; i <= nome.length; i++) {
  console.log(nome[i])
}